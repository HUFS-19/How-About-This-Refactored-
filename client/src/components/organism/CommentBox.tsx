import styled from 'styled-components';

const CommentBox = (productId: number) => {
  return <StyledCommentBox></StyledCommentBox>;
};

const StyledCommentBox = styled.div``;

export default CommentBox;
